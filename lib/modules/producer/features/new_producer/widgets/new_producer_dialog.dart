import 'package:flutter/material.dart';

class NewProducerDialog extends StatelessWidget {
  const NewProducerDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Dialog(
      child: Placeholder(),
    );
  }
}
